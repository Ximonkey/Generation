/***1. Convert Minutes into Seconds.***/
/*
let minutes = prompt("Ingresa los minutos que deseas convertir a segundos");

function toSeconds (minutes){
    return minutes*60;
}
const toSeconds = (minutes) => {
    return minutes*60;
}
let resultado = toSeconds(minutes);
alert(`${minutes} equivale a ${resultado} segundos`);
*/

/***2. Return the Next Number from the Integer Passed.***/
/*
let valor = prompt("Ingresa un numero entero");
let numero = parseInt(valor);

function nextNumber(numero){
    return (numero +1);
}
const nextNumber = (numero) =>{
    return (numero +1);
}
let resultadoNextNumber = nextNumber(numero);
alert(`El resultado de sumar ${numero} + 1 es ${resultadoNextNumber}`);
*/

/***3. Area of a Triangle.***/
/*
let base = prompt("Ingrese la base");
base = parseFloat(base);
let altura = prompt("Ingrese la altura");
altura = parseFloat(altura);

function areaTriangulo (base, altura){
    return (base*altura)/2;
}
const areaTriangulo = (base, altura) =>{
    return (base*altura)/2;
}
let resultadoAreaTriangulo = areaTriangulo(base, altura);
alert(`El área de un triángulo con base ${base} y altura ${altura} es: ${resultadoAreaTriangulo}`);
*/

/***4. Convert Age to Days.***/
/*
let age = prompt("How old are you?");
age = parseInt(age);

function ageToDays (age){
    return age*365;
}
const ageToDays = (age) =>{
    return age*365;
}
let resultAgeToDays = ageToDays(age);
alert(`You have lived ${resultAgeToDays} days.`);
*/

/***5. Power Calculator.***/
/*
let voltage = prompt("Write the voltage");
voltage = parseFloat(voltage);
let current = prompt("Write the current");
current=parseFloat(current);

function powerCalculator (voltage, current){
    return voltage*current;
}
const powerCalculator = (voltage, current) =>{
    return voltage*current;
}
let circuitPower = powerCalculator(voltage, current);
alert(`The power of circuit is ${circuitPower} V`);
*/

/***6. Return Something to Me!.***/
/*
let something = prompt("Complete the statement: Something  _______");

function returnSomething (something){
    return (`Something ${something}`);
}
const returnSomething = (something) => {
    return (`Something ${something}`);
}
let resultSomething = returnSomething(something);
alert(resultSomething);
*/

/***7. Sum of Polygon Angles.***/
/*
let side = prompt("Enter number of sides of a regular polygon");
side = parseInt(side);

function sumOfAngles(side) {
    return 180*(side - 2);
}
const sumOfAngles = (side) => {
    return 180*(side - 2);
}
let totalDegrees = sumOfAngles(side);
alert(`The total sum of internal angles of a polygon of a ${side} sides is ${totalDegrees} degrees`);
*/

/***8. Convert Hours and Minutes into Seconds.***/
/*
let hours = prompt("Enter hours");
hours = parseInt(hours);
let minutes = prompt("Enter minutes");
minutes = parseInt(minutes);

function timeToSeconds (hours, minutes){
    return hours*60*60 + minutes*60;
}
const timeToSeconds = (hours, minutes) => {
    return hours*60*60 + minutes*60;
}
let seconds = timeToSeconds(hours, minutes);
alert(`${hours} hours and ${minutes} minutes is equal to ${seconds} seconds`);
*/
